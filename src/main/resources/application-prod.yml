spring:
  datasource:
    dynamic:
      datasource:
        master: # 主数据库
          url: jdbc:mysql://localhost:3306/edu_trip?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
          username: systek_localhost
          password: systek_admin
          driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    port: 6379 # 端口
    database: 5 # redis第5个库
    host: r-8vb5ulhul3i2w42220.redis.zhangbei.rds.aliyuncs.com # 地址
    password: Systek@!redis # 密码

# swagger开关
swagger:
  enable: false
# 定时任务开关
scheduled:
  enable: true
# 订单流水前缀
code:
  prefix: 11TW

edu:
  file:
    upload-path: /www/file # 线上linux环境文件存储目录
    domain: https://www.systekcn.com # 线上访问域名
unionPay:
  notifyUrl: https://www.systekcn.com/trip/order/unionPayNotify # 银联回调
